replicaCount: 2

image:
  repository: nginx
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  name: ""
  annotations: {}
  path: /
  hosts:
    - host: project-int.local
      paths:
        - /

resources: {}
